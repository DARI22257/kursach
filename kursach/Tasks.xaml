<Window x:Class="kursach.Task"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        xmlns:mvvm="clr-namespace:kursach.VM"
        DataContext="{DynamicResource vm}"
        mc:Ignorable="d"
        Title="Tasks" Height="450" Width="800">
    <Window.Resources>
        <mvvm:TasksMvvm x:Key="vm"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="10" DataContext="{Binding NewEvent}">
            <Label Content="Название"/>
            <TextBox Text="{Binding Title}"/>
            <Label Content="Дата проведения"/>
            <DataGrid Height="20" ItemsSource="{Binding Date}"/>
            <Label Content="Место проведения"/>
            <TextBox Text="{Binding Place}"/>
            <Label Content="Бюджет"/>
            <TextBox Text="{Binding Budget}"/>
            <Label Content="Статус"/>
            <TextBox Text="{Binding Status}"/>
        </StackPanel>

        <ListView Grid.Column="1" ItemsSource="{Binding Events}" SelectedItem="{Binding SelectedEvent}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Title}" Width="80"/>
                    <GridViewColumn Header="Дата проведения" DisplayMemberBinding="{Binding Date}" Width="120"/>
                    <GridViewColumn Header="Место проведения" DisplayMemberBinding="{Binding Place}" Width="120"/>
                    <GridViewColumn Header="Бюджет" DisplayMemberBinding="{Binding Budget}" Width="80"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Status}" Width="80"/>
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="kjk"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>


        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Добавить мероприятие" Command="{Binding InsertEvent}" Width="150" Height="40" />
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Далее" Width="100" Height="40" Click="GoToNextPage_Click" />
        </StackPanel>
    </Grid>
</Window>
